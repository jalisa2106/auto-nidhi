// Re-uses the Admin InsurancePaymentsPage component.
// Backend team: the API calls hit /api/v1/insurance-payments — ensure data_entry role is allowed.
export { default } from '../AdminPages/InsurancePaymentsPage'
